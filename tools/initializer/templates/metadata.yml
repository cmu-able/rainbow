- variables:
    - project_name:
        description: name of the project
        default: example_project
    - probe:
        description: name of your first probe in the system
        default: default_probe
    - gauge:
        description: name of your first gauge in the system
        default: default_gauge
    - effector:
        description: name of your first effector in the system
        default: default_effector
    - logging_level:
        description: Config for Log4J, with levels OFF,FATAL,ERROR,WARN,INFO,DEBUG,TRACE,ALL
        default: DEBUG
    - event_log_path:
        description: path to which events would be logged to
        default: log
    - rainbow_master_location_host:
        description: Location information of the master and this deployment
        default: 127.0.0.1
    - rainbow_deployment_location:
        description: Location information of the deployed delegate
        default: 127.0.0.1
    - rainbow_deployment_environment:
        description: Use "cygwin" for Windows, "linux" for MacOSX
        default: linux
    - rainbow_event_service:
        description: Event infrastructure, type of event middleware rmi | jms | que | eseb
        default: eseb
    - number_of_models:
        description: number of models the target would have
        default: 1
    - number_of_analyses:
        description: number of analyses the target would have
        default: 1
    - number_of_adaption_managers:
        description: number of adaptation managers the target would have
        default: 1
    - number_of_adaption_executors:
        description: number of adaptation executors the target would have
        default: 1
    - number_of_effector_managers:
        description: number of effector managers the target would have
        default: 1
    - utilName:
        description: name of a utility
        default: default_util
    - gaugeTypeName:
        description: name of a gauge type
        default: LoadGaugeT
    - gaugeInstanceName:
        description: name of a gauge instance
        default: LoadG1

- files:
    model/gauges.yml
    model/swim.acme
    model/swim.mtd
    model/tsp.yml
    # model/.acmeproject: templates/model/.acmeproject.ftl
    # model/.project: templates/model/.project.ftl
    'model/opera/Kalman Filter.dtd'
    model/opera/brownout.kalman.config
    model/opera/brownout.model.pxl
    model/opera/opera.config
    stitch/swimStrategies.s
    stitch/swimTactics.t.s
    stitch/utilities.yml
    # stitch/README.txt:

    system/effectors.yml
    system/probes.yml
    system/effectors/defaultEffector.sh
    system/probes/defaultProbe.pl
    system/util/cmdhelper.sh
    rainbow.properties
    rainbow-gui.properties
    ui.yml