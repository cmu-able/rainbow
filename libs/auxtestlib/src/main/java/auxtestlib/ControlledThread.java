package auxtestlib;

/**
 * Class implementing a thread with a <code>myRun</code> method which can throw
 * exceptions. The thread will keep any exception thrown and provides some
 * additional features.
 * 
 * @deprecated use the {@link AbstractControlledThread} instead
 */
@Deprecated
public abstract class ControlledThread extends AbstractControlledThread { // NOPMD
	/*
	 * Dummy class that exists for compatibility.
	 */
}
